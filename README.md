# Elasticsearch Monitor with Prometheus and Grafana 
Docker for Elasticsearch monitoring with Grafana, Prometheus and Elasticsearch Exporter

* run from docker compose up -d -e ES_ENDPOINT=<cluster url>
